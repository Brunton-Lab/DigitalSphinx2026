# Local workstation paths. See configs/paths/template.yaml for an annotated
# template if you are setting up the project on a new machine.
user: ${oc.env:USER}

# Base directories
project_dir: "/home/${paths.user}/Research/MyRepos/DigitalSphinx2026"
base_dir: "/data2/users/${paths.user}/TheSphinx/${version}"
save_dir: ${multirun_save_dir:${paths.base_dir},${run_id}}
log_dir:  "${paths.save_dir}/logs/"
ckpt_dir: "${paths.save_dir}/ckpt/"
fig_dir:  "${paths.save_dir}/figures/"
data_dir: "/data2/users/${paths.user}/TheSphinx/data"

# Submodule paths (relative to project_dir)
body_models_dir: "${paths.project_dir}/sphinx_training/fruitfly_body_models"

# Connectome data paths
connectome_data_dir: "${paths.project_dir}/data/connectome/C_elegans"